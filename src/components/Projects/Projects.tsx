import { Center, Heading } from "@chakra-ui/react";

const Projects = () => {
  return (
    <Center mt={{ base: 20 }}>
      <Heading as="h1">Projects</Heading>
    </Center>
  );
};

export default Projects;
